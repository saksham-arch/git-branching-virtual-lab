const pages = [
  {id:'aim',label:'Aim'}, {id:'objectives',label:'Learning Objectives'}, {id:'overview',label:'Overview'}, {id:'recap',label:'Recap'}, {id:'pretest',label:'Pretest'},
  {id:'branching',label:'Git Branching',group:true}, {id:'branch-concept',label:'Concept',sub:true}, {id:'branch-demo',label:'Interactive Demo',sub:true},
  {id:'merging',label:'Merging',group:true}, {id:'merge-concept',label:'Concept',sub:true}, {id:'merge-practice',label:'Practice',sub:true},
  {id:'conflicts',label:'Conflict Resolution',group:true}, {id:'conflict-lab',label:'Hands-on Lab',sub:true},
  {id:'procedure',label:'Procedure'}, {id:'command-lab',label:'Command Lab'}, {id:'quiz',label:'Quiz'}, {id:'exercise',label:'Exercise'}, {id:'references',label:'Further Readings'}
];
const content = document.getElementById('content'), nav = document.getElementById('sideNav');
const trackablePages=pages.filter(p=>!p.group).map(p=>p.id);
let state = JSON.parse(localStorage.getItem('gitLabState') || '{"completed":[],"commits":1,"branch":"main","feature":0,"merged":false}');
state.completed=state.completed.filter(id=>trackablePages.includes(id));
const save=()=>{localStorage.setItem('gitLabState',JSON.stringify(state)); updateProgress()};
const done=id=>{if(!trackablePages.includes(id))return;if(!state.completed.includes(id)) state.completed.push(id);save()};
const toast=msg=>{const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)};

function renderNav(){nav.innerHTML=pages.map(p=>`<button data-page="${p.id}" class="${p.sub?'sub ':''}${p.group?'group ':''}">${p.label}</button>`).join('');nav.onclick=e=>{const b=e.target.closest('[data-page]');if(b) go(b.dataset.page)}}
function updateProgress(){const pct=Math.round(state.completed.length/trackablePages.length*100);document.getElementById('progressText').textContent=pct+'%';document.getElementById('progressBar').style.width=pct+'%'}
function shell(title,body){return `<h1 class="page-title">${title}</h1>${body}`}
const aim=()=>shell('Git Branching, Merging and Conflict Resolution',`<p class="lead">Learn to develop features safely in parallel, combine work with confidence, and resolve competing changes without losing code.</p><h2>Aim</h2><p>To understand and practise the Git workflow for creating branches, recording independent changes, merging histories, identifying conflicts, and resolving them correctly.</p><div class="outcomes"><div class="outcome"><h3>Branch</h3><p>Isolate work without disturbing the stable code line.</p></div><div class="outcome"><h3>Merge</h3><p>Integrate completed work and understand merge outcomes.</p></div><div class="outcome"><h3>Resolve</h3><p>Read conflict markers, choose the intended result, and finish safely.</p></div></div><div class="callout"><strong>Estimated time:</strong> 35–45 minutes &nbsp; <span class="tag">Beginner friendly</span></div>`);
const objectives=()=>shell('Learning Objectives',`<p>After completing this experiment, the learner will be able to:</p><ol class="objective-list"><li><b>Explain</b> how commits, branches, HEAD, and the working tree relate.</li><li><b>Create and inspect</b> branches using <code>git branch</code>, <code>git switch</code>, and <code>git log</code>.</li><li><b>Differentiate</b> fast-forward, three-way, and squash integration strategies.</li><li><b>Predict</b> whether two histories can merge automatically.</li><li><b>Resolve</b> a content conflict without discarding valid work.</li><li><b>Verify</b> a completed merge through status, diff, graph, and tests.</li></ol><h2>Prerequisites</h2><table class="vl-table"><tr><th>Knowledge</th><th>Required level</th></tr><tr><td>Files, folders, and command-line navigation</td><td>Basic</td></tr><tr><td>Git add, commit, status, and log</td><td>Basic</td></tr><tr><td>Editing a text file</td><td>Basic</td></tr></table><div class="callout"><b>Learning mode:</b> Read the concept, predict the result, perform the simulation, then explain what changed in the commit graph.</div>`);
const overview=()=>shell('Overview',`<p>Git stores your project as a graph of commits. A branch is a movable label pointing to one commit. This makes experimentation cheap: create a branch, work, and later combine it with another line of development.</p><div class="concept-grid"><div class="concept"><h3>Repository</h3><p>The project database containing commits, references, and history.</p></div><div class="concept"><h3>Commit</h3><p>A saved snapshot with a message, author, timestamp, and parent commit.</p></div><div class="concept"><h3>Branch</h3><p>A named pointer that advances as new commits are created.</p></div><div class="concept"><h3>HEAD</h3><p>The pointer indicating the branch or commit currently checked out.</p></div></div><h2>Experiment flow</h2><ol><li>Check your starting knowledge in the pretest.</li><li>Create a feature branch in the interactive demo.</li><li>Compare fast-forward and three-way merges.</li><li>Resolve a realistic content conflict.</li><li>Complete the quiz and practical exercise.</li></ol>`);
const recap=()=>shell('Prerequisite Recap',`<h2>Before you begin</h2><p>You should know the role of the working directory, staging area, and repository.</p><span class="command">git status</span><p>Shows changed, staged, and untracked files.</p><span class="command">git add README.md</span><p>Stages a selected change for the next commit.</p><span class="command">git commit -m "Document setup"</span><p>Creates a commit from staged changes.</p><div class="callout">A commit records staged content—not every file currently changed on your computer.</div>`);
const quizQuestions=[
  {id:'q1',difficulty:'beginner',text:'What does a Git branch primarily represent?',options:['A complete copy of the repository','A movable pointer to a commit','A remote server','A list of untracked files'],answer:1},
  {id:'q2',difficulty:'beginner',text:'Which command creates and switches to a new branch?',options:['git branch --delete feature','git merge feature','git switch -c feature','git status feature'],answer:2},
  {id:'q3',difficulty:'intermediate',text:'When do conflict markers usually appear?',options:['When Git cannot automatically combine overlapping changes','After every commit','Only while cloning','Whenever a branch is created'],answer:0},
  {id:'q4',difficulty:'intermediate',text:'What should happen after resolving all conflict markers?',options:['Delete the repository','Stage the resolved file and complete the merge','Run git clone','Rename main'],answer:1}
];
function renderQuizQuestions(difficulties){return quizQuestions.filter(q=>difficulties.includes(q.difficulty)).map((q,idx)=>`<div class="question" data-difficulty="${q.difficulty}"><p>${idx+1}. ${q.text}</p>${opts(q.id,q.options)}</div>`).join('')}
const quizPage=(pre=false)=>{const title=pre?'Pretest':'Quiz';return shell(title,`<div class="quiz-meta"><label><input type="checkbox" id="filterBeginner" checked> Beginner</label><label><input type="checkbox" id="filterIntermediate" checked> Intermediate</label></div><form id="quizForm"><div id="quizQuestions">${renderQuizQuestions(['beginner','intermediate'])}</div><div class="quiz-result" id="noQuestionsMsg" style="display:none;color:#e67e22"><strong>Please select at least one difficulty level.</strong></div><button class="primary" type="submit" id="quizSubmitBtn">Submit ${title}</button><div class="quiz-result" id="quizResult"></div></form>`)};
function opts(name,arr){return arr.map((x,i)=>`<label class="option"><input type="radio" name="${name}" value="${i}" required> ${String.fromCharCode(97+i)}: ${x}</label>`).join('')}
const branching=()=>shell('Git Branching',`<h2>Why branches?</h2><p>Branches let developers work on features, experiments, and fixes without destabilising the main line. They are lightweight references—not full directory copies.</p><span class="command">git switch -c feature/login</span><span class="command">git branch --show-current</span><h2>A safe workflow</h2><ol><li>Begin from an updated stable branch.</li><li>Create a clearly named branch.</li><li>Make focused commits with meaningful messages.</li><li>Review the diff and test before merging.</li></ol>`);
const branchDemo=()=>shell('Interactive Branching Demo',`<p>Build a miniature commit graph. Commands update the model entirely in your browser.</p><div class="git-stage"><div class="stage-toolbar"><button data-act="branch">Create feature branch</button><button data-act="commit">Commit change</button><button data-act="switch">Switch branch</button><button data-act="reset">Reset demo</button></div><div class="graph" id="graph"></div><div class="terminal" id="terminal">$ git status\nOn branch ${state.branch}\nworking tree clean</div></div><div class="callout">Try: create a branch → commit twice → switch to main. Notice that the branch pointers identify different histories.</div>`);
const merging=()=>shell('Merging',`<h2>Fast-forward merge</h2><p>If the destination branch has not changed, Git can move its pointer directly to the feature branch tip.</p><span class="command">git switch main<br>git merge feature/navigation</span><h2>Three-way merge</h2><p>If both branches contain new commits, Git finds their common ancestor and creates a new merge commit from both lines.</p><div class="concept-grid"><div class="concept"><h3>Fast-forward</h3><p>Linear history; no merge commit is required.</p></div><div class="concept"><h3>Three-way</h3><p>Diverged history; combines two tips with their shared base.</p></div></div><div class="callout"><strong>Important:</strong> A successful automatic merge means Git combined text mechanically. You must still review and test the result.</div>`);
const mergePractice=()=>shell('Merge Practice',`<p>Use the branch simulator first, then merge the feature work into <code>main</code>.</p><div class="git-stage"><div class="stage-toolbar"><button data-act="prepare">Prepare diverged history</button><button data-act="merge">Run git merge feature</button><button data-act="reset">Reset demo</button></div><div class="graph" id="graph"></div><div class="terminal" id="terminal">$ Choose “Prepare diverged history” to begin.</div></div>`);
const conflicts=()=>shell('Conflict Resolution',`<h2>Why conflicts happen</h2><p>A conflict occurs when Git has multiple plausible results and cannot safely decide which content should survive. It pauses the merge and asks you to make the decision.</p><div class="conflict">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD<br>Navigation: Home | Experiments | About<br>=======<br>Navigation: Home | Labs | Contact<br>&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature/navigation</div><h2>Resolution process</h2><ol><li>Run <code>git status</code> to identify conflicted files.</li><li>Open each file and understand both changes.</li><li>Edit to the correct final content and remove every marker.</li><li>Stage the resolved file with <code>git add</code>.</li><li>Complete the merge commit and test.</li></ol><span class="command">git diff --check<br>git add index.html<br>git commit</span>`);
const conflictLab=()=>shell('Hands-on Conflict Lab',`<p>Two branches changed the same navigation line. Produce a sensible final version, then validate it.</p><div class="file-editor"><div class="editor-pane"><h3>Conflicted file</h3><div class="conflict">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD<br>Navigation: Home | Experiments | About<br>=======<br>Navigation: Home | Labs | Contact<br>&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature/navigation</div></div><div class="editor-pane"><h3>Your resolved file</h3><textarea id="resolution" spellcheck="false" placeholder="Write the final navigation line here..."></textarea></div></div><button class="primary" id="validateConflict">Validate resolution</button><div class="quiz-result" id="conflictResult"></div>`);
const procedure=()=>shell('Procedure',`<p>Perform the experiment in order. Each observation connects a command to a change in the commit graph.</p><div class="procedure"><section><b>01</b><div><h3>Prepare the repository</h3><span class="command">mkdir git-lab && cd git-lab<br>git init<br>git branch -M main</span><p>Create <code>notes.txt</code>, stage it, and record the base commit.</p></div></section><section><b>02</b><div><h3>Create isolated work</h3><span class="command">git switch -c feature/summary<br>echo "Feature summary" >> notes.txt<br>git add notes.txt && git commit -m "Add summary"</span><p>Observe that <code>feature/summary</code> advances while <code>main</code> remains at the base.</p></div></section><section><b>03</b><div><h3>Inspect and merge</h3><span class="command">git log --graph --oneline --decorate --all<br>git switch main<br>git merge feature/summary</span><p>Because main has not advanced, the result is a fast-forward.</p></div></section><section><b>04</b><div><h3>Force a controlled conflict</h3><p>Create two branches from the same base and edit the same line differently. Merge them and examine all three marker regions.</p></div></section><section><b>05</b><div><h3>Resolve and verify</h3><span class="command">git diff --check<br>git add notes.txt<br>git commit<br>git status<br>git log --graph --oneline --all</span><p>Confirm that no marker remains, the working tree is clean, and the graph contains the expected merge.</p></div></section></div>`);
const commandLab=()=>shell('Command Lab',`<p>Complete the guided terminal sequence. Enter the command that matches each requested action.</p><div class="lab-status"><span>Step <b id="labStep">1</b> of 6</span><span id="labScore">0 correct</span></div><div class="terminal guided" id="guidedTerminal"><div id="labPrompt">Create and switch to a branch named <b>feature/navbar</b>.</div><label><span>$</span><input id="commandInput" autocomplete="off" spellcheck="false" placeholder="type a Git command"></label><div id="commandFeedback"></div></div><div class="stage-toolbar"><button id="runCommand">Run command</button><button id="showHint">Show hint</button><button id="restartLab">Restart lab</button></div><h2>Command reference</h2><table class="vl-table"><tr><th>Goal</th><th>Command</th></tr><tr><td>List local branches</td><td><code>git branch</code></td></tr><tr><td>Create and switch</td><td><code>git switch -c &lt;name&gt;</code></td></tr><tr><td>Show decorated graph</td><td><code>git log --graph --oneline --decorate --all</code></td></tr><tr><td>Merge into current branch</td><td><code>git merge &lt;branch&gt;</code></td></tr><tr><td>Cancel a conflicted merge</td><td><code>git merge --abort</code></td></tr></table>`);
const exercise=()=>shell('Exercise',`<p>Complete this workflow in any practice repository. Check each step as you finish.</p><div class="checklist" id="checklist"><label><input type="checkbox"> Create a repository and make an initial commit on <code>main</code>.</label><label><input type="checkbox"> Create <code>feature/profile</code> and commit a profile heading.</label><label><input type="checkbox"> Return to <code>main</code> and change the same heading differently.</label><label><input type="checkbox"> Merge <code>feature/profile</code> and inspect the conflict.</label><label><input type="checkbox"> Resolve the content intentionally, stage it, and finish the merge.</label><label><input type="checkbox"> Draw or inspect the history using <code>git log --graph --oneline --all</code>.</label></div><div class="callout"><strong>Expected evidence:</strong> command history, the final resolved file, and a commit graph showing both branches joining.</div>`);
const references=()=>shell('Further Readings',`<h2>Reference material</h2><ul class="reading-list"><li><a href="https://git-scm.com/book/en/v2/Git-Branching-Branches-in-a-Nutshell" target="_blank" rel="noreferrer">Pro Git — Branches in a Nutshell</a><span>Branch pointers, HEAD, and commit graphs</span></li><li><a href="https://git-scm.com/docs/git-merge" target="_blank" rel="noreferrer">Git merge documentation</a><span>Merge modes, options, and stopping a merge</span></li><li><a href="https://git-scm.com/docs/git-switch" target="_blank" rel="noreferrer">Git switch documentation</a><span>Creating and changing branches safely</span></li><li><a href="https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/addressing-merge-conflicts" target="_blank" rel="noreferrer">GitHub — Addressing merge conflicts</a><span>Conflict workflow in collaborative repositories</span></li></ul><h2>Key takeaways</h2><ul><li>A branch is a pointer, not a separate copy of every file.</li><li>Merge the source branch while checked out on the destination branch.</li><li>Conflict resolution is a design decision, not merely marker deletion.</li><li>Always inspect and test the final combined result.</li></ul><div class="callout"><b>Completion criterion:</b> You should be able to draw the before-and-after graph of a branch and merge without running Git.</div>`);

const renderers={aim,objectives,overview,recap,pretest:()=>quizPage(true),branching,'branch-concept':branching,'branch-demo':branchDemo,merging,'merge-concept':merging,'merge-practice':mergePractice,conflicts,'conflict-lab':conflictLab,procedure,'command-lab':commandLab,quiz:()=>quizPage(false),exercise,references};
function go(id){if(!renderers[id]) id='aim';content.innerHTML=renderers[id]();document.querySelectorAll('[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===id));history.replaceState(null,'','#'+id);document.getElementById('sidebar').classList.remove('open');content.focus();wire(id);if(!['pretest','quiz','conflict-lab','exercise','branch-demo','merge-practice'].includes(id))done(id)}
function graph(){const el=document.getElementById('graph');if(!el)return;let main='<div class="commit-row">';for(let i=0;i<state.commits;i++)main+=`<div class="commit">M${i+1}</div>`;main+=`<span class="branch-label" style="left:${25+(state.commits-1)*100}px;top:0">main</span></div>`;let feature='';if(state.feature){feature='<div class="commit-row">'+Array.from({length:state.feature},(_,i)=>`<div class="commit feature">F${i+1}</div>`).join('')+`<span class="branch-label">feature</span></div>`}el.innerHTML=main+feature}
function wire(id){graph();document.querySelector('.stage-toolbar')?.addEventListener('click',e=>{const a=e.target.dataset.act;if(!a)return;const term=document.getElementById('terminal');if(a==='reset'){state.commits=1;state.feature=0;state.branch='main';state.merged=false;term.textContent='$ Demo reset.'}if(a==='branch'){state.branch='feature';state.feature=Math.max(1,state.feature);term.innerHTML='$ git switch -c feature<br><span class="ok">Switched to a new branch \'feature\'</span>'}if(a==='commit'){if(state.branch==='feature')state.feature++;else state.commits++;term.innerHTML='$ git commit -m "Update project"<br><span class="ok">['+state.branch+'] commit created</span>'}if(a==='switch'){state.branch=state.branch==='main'?'feature':'main';if(state.branch==='feature'&&!state.feature)state.feature=1;term.innerHTML='$ git switch '+state.branch+'<br><span class="ok">Switched to branch \' '+state.branch+' \'</span>'}if(a==='prepare'){state.commits=3;state.feature=2;state.branch='main';state.merged=false;term.textContent='$ History prepared: main and feature have diverged.'}if(a==='merge'){if(!state.feature){term.textContent='$ Nothing to merge. Prepare history first.'}else{state.commits++;state.merged=true;term.innerHTML='$ git merge feature<br><span class="ok">Merge made by the ort strategy.</span>';done(id)}}save();graph()});
  const q=document.getElementById('quizForm');if(q){const fb=document.getElementById('filterBeginner'),fi=document.getElementById('filterIntermediate'),qc=document.getElementById('quizQuestions'),noMsg=document.getElementById('noQuestionsMsg'),submitBtn=document.getElementById('quizSubmitBtn');const applyFilter=()=>{const diffs=[];if(fb.checked)diffs.push('beginner');if(fi.checked)diffs.push('intermediate');qc.innerHTML=renderQuizQuestions(diffs);const hasQ=diffs.length>0;noMsg.style.display=hasQ?'none':'block';submitBtn.disabled=!hasQ;document.getElementById('quizResult').style.display='none'};fb.onchange=applyFilter;fi.onchange=applyFilter;q.onsubmit=e=>{e.preventDefault();const f=new FormData(q),diffs=[];if(fb.checked)diffs.push('beginner');if(fi.checked)diffs.push('intermediate');const visible=quizQuestions.filter(qq=>diffs.includes(qq.difficulty)),total=visible.length,score=visible.reduce((s,qq)=>s+(Number(f.get(qq.id))===qq.answer?1:0),0),r=document.getElementById('quizResult');r.style.display='block';r.innerHTML=`<strong>Score: ${score}/${total}</strong><br>${score===total?'Excellent—every answer is correct.':score>=total-1?'Good work. Review the item you missed and try again.':'Review the concept pages, then retry the quiz.'}`;if(score>=total-1)done(id)}}
  const v=document.getElementById('validateConflict');if(v)v.onclick=()=>{const val=document.getElementById('resolution').value.trim(),r=document.getElementById('conflictResult'),valid=val.includes('Navigation:')&&!/[<=>]{7}/.test(val)&&/Home/i.test(val);r.style.display='block';r.innerHTML=valid?'<strong>Resolution accepted.</strong> Conflict markers are gone and the result preserves a valid navigation line. Remember to stage and commit it.':'<strong>Not resolved yet.</strong> Remove all conflict markers and produce one final navigation line beginning with “Navigation:”.';if(valid)done(id)};
  const run=document.getElementById('runCommand');if(run){const tasks=[
    {prompt:'Create and switch to a branch named <b>feature/navbar</b>.',answers:['git switch -c feature/navbar','git checkout -b feature/navbar'],hint:'Use git switch with the create flag: -c.'},
    {prompt:'Stage the changed file named <b>nav.html</b>.',answers:['git add nav.html'],hint:'The staging command begins with git add.'},
    {prompt:'Commit it with the message <b>Add navbar</b>.',answers:['git commit -m "Add navbar"',"git commit -m 'Add navbar'"],hint:'Use git commit -m followed by the message in quotes.'},
    {prompt:'Switch back to the <b>main</b> branch.',answers:['git switch main','git checkout main'],hint:'Use git switch followed by the branch name.'},
    {prompt:'Merge <b>feature/navbar</b> into the current branch.',answers:['git merge feature/navbar'],hint:'The current branch is the destination; name the source after git merge.'},
    {prompt:'Display a compact graph of every branch.',answers:['git log --graph --oneline --all','git log --oneline --graph --all'],hint:'Combine git log with --graph, --oneline, and --all.'}
  ];let step=0,score=0;const input=document.getElementById('commandInput'),feedback=document.getElementById('commandFeedback'),prompt=document.getElementById('labPrompt');const paint=()=>{document.getElementById('labStep').textContent=Math.min(step+1,tasks.length);document.getElementById('labScore').textContent=score+' correct';prompt.innerHTML=step<tasks.length?tasks[step].prompt:'<b>Lab complete.</b> You created, committed, merged, and inspected a branch.';input.disabled=step>=tasks.length;run.disabled=step>=tasks.length;if(step>=tasks.length)done(id)};run.onclick=()=>{const val=input.value.trim().replace(/\s+/g,' '),ok=tasks[step].answers.includes(val);feedback.className=ok?'ok':'error';feedback.textContent=ok?'Correct. The repository state advances to the next step.':'That command does not perform the requested action. Try again or reveal the hint.';if(ok){score++;step++;input.value='';setTimeout(()=>{feedback.textContent='';paint();input.focus()},500)}};input.onkeydown=e=>{if(e.key==='Enter')run.click()};document.getElementById('showHint').onclick=()=>{feedback.className='hint';feedback.textContent='Hint: '+tasks[step].hint};document.getElementById('restartLab').onclick=()=>{step=0;score=0;input.disabled=false;run.disabled=false;input.value='';feedback.textContent='';paint()};paint()}
  const c=document.getElementById('checklist');if(c)c.onchange=()=>{const boxes=[...c.querySelectorAll('input')];if(boxes.every(x=>x.checked)){done(id);toast('Exercise complete!')}}}

renderNav();updateProgress();document.getElementById('menuButton').onclick=()=>document.getElementById('sidebar').classList.toggle('open');document.getElementById('rateButton').onclick=()=>{document.getElementById('modalContent').innerHTML='<h2>Rate this experiment</h2><p class="stars">★★★★★</p><p>Thank you for helping improve this learning experience.</p>';document.getElementById('modal').showModal()};document.getElementById('bugButton').onclick=()=>{document.getElementById('modalContent').innerHTML='<h2>Report a Bug</h2><p>Describe what happened and include the page name and steps to reproduce.</p><a class="primary" href="https://github.com/saksham-arch/git-branching-virtual-lab/issues/new" target="_blank">Open GitHub issue</a>';document.getElementById('modal').showModal()};document.querySelector('#modal .close').onclick=()=>document.getElementById('modal').close();window.addEventListener('hashchange',()=>go(location.hash.slice(1)));go(location.hash.slice(1)||'aim');
